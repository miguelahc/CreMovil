import 'package:flutter/cupertino.dart';

const PrimaryColor = Color(0XFF2b5c5a);
const SecondaryColor = Color(0XFF84bd00);
const DarkColor = Color(0XFF3a3d5f);

const PrimaryGradient =
    LinearGradient(colors: [Color(0XFF618A02), Color(0XFF84BD00)],
        begin: Alignment.bottomLeft,
        end: Alignment.topRight);

const SecondaryGradient =
    LinearGradient(colors: [Color(0XFF3a3d5f), Color(0XFF84BD00)],
        begin: Alignment.bottomLeft,
        end: Alignment.topRight);

const DarkGradient =
    LinearGradient(colors: [Color(0XFF3a3d5f), Color(0XFF2b5c5a)],
        begin:  Alignment.bottomLeft,
        end: Alignment.topRight);
