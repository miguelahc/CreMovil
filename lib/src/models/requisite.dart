class Requisite {
  int id;
  String title;
  int idImage;
  String coverImage;
  int width;
  int height;

  Requisite(this.id, this.title, this.idImage, this.coverImage, this.width,
      this.height);
}
