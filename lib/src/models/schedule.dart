class Schedule {
  int pointId;
  String pointName;
  int id;
  String description;
  String fromAM;
  String untilAM;
  String fromPM;
  String untilPM;

  Schedule(this.pointId, this.pointName, this.id, this.description, this.fromAM,
      this.untilAM, this.fromPM, this.untilPM);
}
