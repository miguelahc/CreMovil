class DidYouKnow {
  int id;
  String title;
  int idImage;
  String coverImage;
  int width;
  int height;

  DidYouKnow(this.id, this.title, this.idImage, this.coverImage, this.width,
      this.height);
}
